# Rigor scorecard — BINANCE:BTCUSDT D

**Grade: Likely overfit** (31/100) — This is an always-in-market Supertrend reversal swing strategy — medium holding periods, no stops, position rides until the next flip. The decisive failure is D1: in-sample PF 2.54 collapses to OOS PF 0.66 across 36 trades, meaning the strategy actively loses money on unseen data — not marginal degradation but a full edge reversal, the clearest hallmark of curve-fitting. This is compounded by L4 FAIL (zero commission and zero slippage in source), which means the already-collapsed OOS figure is still flattering the real result; multi-instrument and multi-timeframe passes suggest a genuine direc

> Scores how *trustworthy* the backtest is, not whether the strategy is profitable.

*Engine: modelled 'supertrend' on free OHLCV (BTC-USD, 3652 bars, D); a faithful re-implementation of the logic, not the literal Pine on TradingView. archetype inferred via llm (confidence 99%). Pine syntax not verified (no TradingView connected); rigor reflects the strategy logic.*

## Tier 1 — hard gates

| Gate | Result | Finding |
|------|--------|---------|
| G1 Look-ahead bias | PASS | no future-data access detected (no lookahead_on, no unguarded MTF read). |
| G2 Repainting | PASS | evaluates on confirmed bars (calc_on_every_tick default false). |
| G3 Sufficient sample | PASS | 102 trades — enough to draw a conclusion. |
| G4 Friction illusion | PASS | edge survives realistic costs (PF 1.83 -> 1.80). |

## Tier 2 — scored dimensions

| Dimension | Result | Finding |
|-----------|--------|---------|
| D1 Out-of-sample | FAIL | edge degrades out-of-sample: in-sample PF 2.54 -> OOS PF 0.66 (36 OOS trades) — sign of curve-fitting. |
| D2 Overfitting surface | PASS | 2 tunable inputs vs 102 trades (51 trades/param) — low degrees of freedom. |
| D3 Multi-timeframe | PASS | edge persists on a neighbouring timeframe (240: PF 1.06, W: PF 7.00) — not a single-TF cherry-pick. |
| D4 Multi-instrument | PASS | edge generalizes to another instrument (NASDAQ:QQQ: PF 1.24). |
| D5 Inverse-edge / luck | PASS | the inverse strategy loses (inverse PF 0.55 vs strategy PF 1.83) — the directional edge is real, not coin-flip. |
| D6 Risk profile | PASS | return/drawdown 4.6 (net +532.04% vs max DD 114.60%). |
| D7 Cost survival | PASS | edge mostly survives realistic costs (PF 1.83 -> 1.80, gave back 0.03). |

## Deep review (Sonnet 4.6 — advisory, not scored)

| Check | Result | Finding |
|-------|--------|---------|
| L1 Look-ahead / repaint | PASS | No security()/request.*() calls; strategy() defaults calc_on_every_tick=false, so ta.supertrend and ta.change(dir) are evaluated on confirmed bar-close values only. |
| L2 Overfitting | WARN | Only two tunable inputs (atrPeriod=10, factor=3.0) — low count, but both are trivially grid-searched; no out-of-sample window is enforced, so reported results likely reflect the optimised pair for the visible history. |
| L3 Cherry-picking | PASS | No hardcoded date ranges, session filters, or symbol-specific conditionals; the strategy is symbol- and timeframe-agnostic on its face. |
| L4 Cost & fill realism | FAIL | strategy() is declared with no commission_type, commission_value, or slippage arguments, so both default to zero; strategy.entry() fills at next-bar open with no slippage model, overstating net P&L. |
| L5 Position sizing / risk | WARN | strategy.entry() uses no explicit qty or default_qty_type/default_qty_value, so lot size is platform-default (1 contract); there is no stop-loss — the only exit is the opposing strategy.entry(), meaning losses ride until the next Supertrend flip. |
| L6 Logic soundness | PASS | ta.change(dir) < 0 correctly detects the dir 1→−1 transition (bullish flip) for Long entry and > 0 detects −1→1 (bearish flip) for Short entry; the always-in-market reversal mechanic is internally consistent with the Supertrend definition. |

---
*Generated by the TradingHypothesisLab rigor checker. Gates are pass/fail disqualifiers; dimensions are scored. Empirical checks run a faithful Python re-implementation of the strategy on free OHLCV. See docs/rigor_rubric.md.*
